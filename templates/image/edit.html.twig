{% extends 'base.html.twig' %}

{% block title %}Editer une image{% endblock %}

{% block body %}
    <h1>Edition d'image</h1>

    {{ include('image/_form.html.twig', {'button_label': 'Update'}) }}

    <button class="btn btn-lg btn-info"><a href="{{ path('app_image_index') }}">Retour à la liste</a></button>

    {{ include('image/_delete_form.html.twig') }}
{% endblock %}
