<div class="card">
    <div class="card-body">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button class="btn btn-success">{{ button_label|default('Publier le dessin') }}</button>
        {{ form_end(form) }}
    </div>
</div>
